extends layout

block content
  h1= article.title
  h5 Written by: #{article.author}
  p= article.body
  hr
  a.btn.btn-secondary(href='/articles/edit/'+article._id) Edit Post
  button.btn.btn-danger.delete-art(href='#', data-id=article._id) Delete